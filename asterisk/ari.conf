; NOTE: Docker renders ari.conf from a template (see asterisk/conf/ari.conf.template)
; For native installs, edit the user/password below.

[general]
enabled = yes
pretty = yes
allowed_origins = *

[ariuser]
type = user
read_only = no
password = aripass
